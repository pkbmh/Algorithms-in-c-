#include <bits/stdc++.h>
using namespace std;
#define ARRAY_SIZE(arr) sizeof(arr) / sizeof(arr[0])

int main()
{
    int arr[] = {6, 7, 8, 1, 2, 3, 9, 10};
    int n = ARRAY_SIZE(arr);


return 0;
}
